# Measured results

Measured external-holdout results are generated only after a verified real-data training run. On Windows, run:

```powershell
.\scripts\showcase.cmd
```

`scripts/build_showcase.py` then generates this document, the README result block and the associated visual artifacts from integrity-checked evaluation evidence.
